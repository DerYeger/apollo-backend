ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [
            eu.yeger.apollo.ApplicationKt.mainModule,
            eu.yeger.apollo.routing.RoutingKt.routingModule
        ]
    }
}
